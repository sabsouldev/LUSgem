<form method="post" action="{{ path('podcast.delete', {'id': podcast.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ podcast.id) }}">
    <button class="btn"class="btn btn-primary" style="background-color: #D199B6" >Supprimer</button>
</form>