{% extends 'base.html.twig' %}

{% block title %}Hello PartenairesController!{% endblock %}

{% block body %}
<div class="container">
    <section class="row  mx-auto">
        <div class="row col-12 mx-auto">
            <h1>Partenaires</h1>
        </div>
        {% for partenaire in partenaires %}
        <div class="col g-1 py-3">   
            <div class="card mb-3 mx-auto" style="width: 18rem;">
                <img src="{{ asset('upload/'~ partenaires.illustration)}}" class="card-img-top" alt="...">
                <div class="card-body text-center">
                    <h5 class="card-title text-center ">{{partenaires.name}}</h5>
                    <a href="{{path('partenaires.show', {'id': partenaires.id})}}" class="btn btn-primary" style="background-color: #D199B6" >Voir plus</a>
                </div>
            </div>
        </div>
        {% endfor %}
    </section>
</div>
{% endblock %}
